# Gitignore for a Node.js / Express / EJS project

[Dependencies]
# The node_modules folder contains all third-party packages.
# It can be huge and is always regenerated by running `npm install`.
# This is the most important line in any Node.js gitignore.
node_modules/

[Logs]
# Log files generated by the application or npm.
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

[Environment Variables]
# Environment variable files contain secrets like database connection strings
# and API keys. They should NEVER be committed to Git.
# Each developer should have their own .env file.
.env
.env.local
.env.development.local
.env.production.local
.env.test.local
.env.*

[Build & Distribution]
# If you ever use a build process (e.g., with TypeScript or a front-end framework),
# the output folders are usually called 'dist' or 'build'.
dist/
build/
.next/
.vercel/

[Operating System Files]
# Files generated by macOS, Windows, and Linux systems.
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

[IDE & Editor Config]
# Configuration files for popular code editors.
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
.project
.settings/

[Test & Coverage Reports]
# If you add testing to your project, coverage reports are generated here.
coverage/
.nyc_output/