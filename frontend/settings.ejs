<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.name %> - Settings</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <link rel="stylesheet" href="/css/themes.css">
    <link rel="stylesheet" href="/css/index.css">

</head>
<body class="<%= userProfile.preferences.theme === 'dark' ? 'dark-theme' : '' %>">

    <header>
        <div class="container">
            <nav class="navbar">
                <a href="/index" class="navbar-brand" style="color: <%= userProfile.preferences.fontColor %>;">
                    <i class="fas fa-book-open"></i> <%= user.displayName %>'s Notes
                </a>
                <ul class="navbar-nav">
                    <li><a href="/index" class="nav-link">Home</a></li>
                    <li><a href="/settings" class="nav-link active">Settings</a></li> <li><a href="/logout" class="nav-link">Logout</a></li>
                </ul>
                <button class="navbar-toggler" id="navbarToggler" aria-label="Toggle navigation">
                    <i class="fas fa-bars"></i>
                </button>
            </nav>
        </div>
    </header>

    <main class="container" style="margin-top: 30px;">
        <div class="settings-form">
            <h2 style="color: <%= userProfile.preferences.fontColor %>;">User Settings & Preferences</h2>
            
            <form action="/settings" method="POST">
                <h3>Profile Information</h3>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" value="<%= user.email %>" required>
                </div>

                <hr style="margin: 30px 0;">
                
                <h2 style="color: <%= user.preferences.fontColor %>;">Theme</h2>
                <div class="form-group">
                    <div class="theme-switcher">
                        <i class="fas fa-sun"></i>
                        <label class="switch">
                            <input type="checkbox" id="themeToggle" <%= user.preferences.theme === 'dark' ? 'checked' : '' %>>
                            <span class="slider round"></span>
                        </label>
                        <i class="fas fa-moon"></i>
                    </div>
                    <input type="hidden" name="theme" id="themeValue" value="<%= user.preferences.theme %>">
                </div>

                <h3>Preferences</h3>
                <div class="form-group">
                    <label for="noteBackground">Note Background Color</label>
                    <input type="color" id="noteBackground" name="noteBackground" value="<%= userProfile.preferences.noteBackground %>">
                    <small>Current: <%= userProfile.preferences.noteBackground %></small>
                </div>
                <div class="form-group">
                    <label for="fontColor">Font Color</label>
                    <input type="color" id="fontColor" name="fontColor" value="<%= userProfile.preferences.fontColor %>">
                     <small>Current: <%= userProfile.preferences.fontColor %></small>
                </div>
                
                <button type="submit" class="btn btn-primary" style="background-color: <%= userProfile.preferences.noteBackground %>; color: <%= userProfile.preferences.fontColor %>;">Save Settings</button>
            </form>
        </div>
    </main>

    <%- include('partials/footer') %>

    <script src="/javascript/script.js"></script>
</body>
</html>